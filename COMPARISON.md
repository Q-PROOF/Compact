# COMPARISON.md — the competitor matrix, with evidence labels

Generated by `python scripts/gen_comparison.py` from the
committed `results/` artifacts.  Every row states where its
numbers come from and how to reproduce them.  Rows are labeled:

- **[measured]** — run in this repository, artifact committed,
  regenerable via `python scripts/run_benchmarks.py` (or the
  producer script named in the row).
- **[referee]** — an external tool checking Compact's outputs
  (not a performance comparison).
- **[literature]** — numbers reported by the compared tool's own
  documentation/papers.  NOT apples-to-apples with [measured]
  rows; shown for context only.
- **[not runnable here]** — the tool could not be executed in
  this environment; no numbers are claimed.

## Same-input, same-gate-set matrix — [measured]

Source: [`results/repro.json`](results/repro.json) + repro.md + repro.csv, produced by `python scripts/repro_harness.py`, suite pinned by [`results/MANIFEST.json`](results/MANIFEST.json), commit `ccde0ec41dd8`.

Method: identical inputs for every tool, every output lowered
to u3+cx before counting, every output refereed by Qiskit's
Operator.  Wall time includes compactq's in-product proof.

| tool | mean 2q cut | median wall ms | evidence |
|---|---:|---:|---|
| compact | 10.2% | 22 | [measured] |
| qiskit | 14.6% | 7 | [measured] |
| pytket | 25.0% | 315 | [measured] |
| cirq | 2.8% | 105 | [measured] |

compact vs qiskit 2q head-to-head (W/T/L): **2 / 12 / 1** [measured]

## BQSKit head-to-head — [measured]

Source: [`results/bqskit.json`](results/bqskit.json) (`python scripts/bqskit_bench.py`), BQSKit 1.2.1, commit `53d00ca20e3f`.

32 QASMBench small circuits, BQSKit's documented `bqskit.compile` pipeline, every output refereed: **32 circuits**.  Per-circuit records in the artifact.

## Full-transpilation pipeline — [measured]

Source: [`results/fulltranspile.json`](results/fulltranspile.json) (`python scripts/fulltranspile_bench.py`): compact vs qiskit end-to-end pipelines, 0 W / 4 T / 9 L (see artifact protocol).

## External referees checking Compact — [referee]

- PyZX full_reduce cross-check (42 circuits): 27 OK; 13 skipped; 1 error TypeError: Phase expression '1.3917358338339233e-07' contains unbound variables; 1 error TypeError: Phase expression '-1.58730875641222e-05' contains unbound variables — the two phase-expression errors are a PyZX parser limitation, not inequivalence ([`results/pyzx_referee.json`](results/pyzx_referee.json), `python scripts/referee_pyzx.py`) [referee]
- MQT QCEC cross-check (42 circuits): 16 OK (up to global phase); 11 skipped; 10 OK; 2 inconclusive; 2 referee-disagreement (Operator equivalent within 1e-6; QCEC tolerance stricter) — the two referee disagreements are QCEC's stricter phase tolerance; Compact's Operator fidelity is 1.0/0.99999999997 on those ([`results/qcec_referee.json`](results/qcec_referee.json), `python scripts/referee_qcec.py`) [referee]

## Latency — [measured]

Source: [`results/latency.json`](results/latency.json) (`python scripts/latency_bench.py`): compactq import ~26.48 ms vs qiskit ~1049.44 ms; optimize with proof p50 ~1.211 ms / p95 ~2.155 ms on the small circuit corpus (import time: best-of-5 fresh-process spawns; optimize wall: 200 calls on a fixed 3q/12-gate circuit, proof included).

## Proof-ceiling families (DD prover) — [measured]

Source: [`results/dd_ceiling.json`](results/dd_ceiling.json) (`python scripts/dd_ceiling_bench.py`): exact decision-diagram proofs, QFT family proven to 18q at the default 400k-node budget with per-width node counts and wall times; before/after v0.2.3 deltas in the artifact.  Per-tier scope definitions: [VERIFICATION.md](VERIFICATION.md).

## Tools not measured in this repository — [not runnable here]

- **VOQC / Quartz / GUOQ**: no runnable build in this
  environment at release time; no numbers are claimed.
  (Comparator dispositions recorded in
  [`results/fulltranspile.md`](results/fulltranspile.md) where attempted.)

## Literature context — [literature]

- Qiskit / TKET / BQSKit headline numbers quoted anywhere in
  issues or docs from their own papers/docs are context only,
  never merged into the [measured] tables above.

The honest summary: Compact's differentiator is the in-product,
tiered proof attached to every output — the point of this file
is that every other number here can be checked, by anyone, from
a clean clone.
