"""compactq-check — independent certificate checker.

Checks that two OpenQASM 2.0 circuits are equivalent as claimed by a
Compact compilation certificate.  Written fresh from the certificate
specification: it shares NO code with compactq and imports nothing from
it.  Zero dependencies, Python >= 3.9.
"""
__version__ = "0.1.0"
