# Part of the compactq Benchpress integration (github.com/Q-PROOF/Compact).
# License: MIT (compactq).  This gym follows the Benchpress gym layout so
# it can be upstreamed as-is.
"""compactq gym for Qiskit Benchpress."""
from benchpress.config import Configuration

Configuration.gym_name = "compactq"
